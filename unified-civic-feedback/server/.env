# PostgreSQL credentials
POSTGRES_USER=ucffi_user
POSTGRES_PASSWORD=ucffi_pass
POSTGRES_DB=ucffi_db

# Compose will substitute these into DATABASE_URL
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?sslmode=disable

# Application port (Gin server)
PORT=3000
